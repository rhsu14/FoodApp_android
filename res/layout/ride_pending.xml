<RelativeLayout
    android:id="@+id/Ride_Pending"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Ride_Pending" >

    <TextView
        android:id="@+id/Ride_Pending_Text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="56dp"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Button
        android:id="@+id/Ride_Pending_Accept"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/Ride_Pending_Text"
        android:layout_below="@+id/Ride_Pending_Text"
        android:layout_marginTop="91dp"
        android:text="@string/Ride_Pending_Accept" 
        android:onClick="accept" />

    <Button
        android:id="@+id/Ride_Pending_Deny"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/Ride_Pending_Accept"
        android:layout_below="@+id/Ride_Pending_Accept"
        android:layout_marginTop="70dp"
        android:text="@string/Ride_Pending_Deny"
        android:onClick="deny" />

</RelativeLayout>
